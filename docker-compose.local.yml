# Local development override
name: teslapp-local
services:
  webapp:
    ports:
      - '50079:80'
    networks:
      shared:
        ipv4_address: 172.22.0.11

networks:
  shared:
    external: false
    ipam:
      config:
        - subnet: 172.22.0.0/16
