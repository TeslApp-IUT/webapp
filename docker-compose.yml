name: teslapp
services:
  webapp:
    build: .
    ports:
      - '50025:80'
    env_file: .env
    restart: unless-stopped
    networks:
      shared:
        ipv4_address: 172.22.0.6

networks:
  shared:
    name: teslapp_shared
    external: true
