worker_processes auto;
error_log /dev/stderr warn;
pid /run/nginx/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    access_log /dev/stdout;
    sendfile on;
    tcp_nopush on;
    keepalive_timeout 65;
    server_tokens off;

    gzip on;
    gzip_comp_level 5;
    gzip_types text/html text/css application/javascript application/json
               text/xml application/xml image/svg+xml;

    server {
        listen 80;
        root /var/www/html;
        index index.php;

        # Force HTTPS when served behind a reverse proxy (X-Forwarded-Proto).
        if ($http_x_forwarded_proto = "http") {
            return 301 https://$host$request_uri;
        }

        # OWASP security headers (production-strict).
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
        add_header X-Frame-Options "DENY" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "0" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'" always;
        add_header Permissions-Policy "geolocation=(self), camera=(), microphone=(), payment=(), usb=()" always;

        # Canonical redirect: /site/home → /
        location = /site/home {
            return 301 /;
        }

        # Block dot-files (e.g. .htaccess, .env).
        location ~ /\. {
            deny all;
        }

        # Front controller: serve real files/dirs, otherwise route through index.php.
        location / {
            try_files $uri $uri/ /index.php$is_args$args;
        }

        # PHP-FPM handler.
        location ~ \.php$ {
            fastcgi_split_path_info ^(.+\.php)(/.*)$;
            fastcgi_pass 127.0.0.1:9000;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            fastcgi_hide_header X-Powered-By;
            include fastcgi_params;
        }
    }
}
