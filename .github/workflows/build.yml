name: Build

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonarqube:
    name: SonarQube
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          fetch-depth: 0

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@7006c4492b2e0ee0f816d36501671557c97f5995 # v8.1.0
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_DB_URL: ${{ secrets.SONAR_JDBC_URL }}
          SONAR_DB_USER: ${{ secrets.SONAR_JDBC_USER }}
          SONAR_DB_PASSWORD: ${{ secrets.SONAR_JDBC_PASSWORD }}
        with:
          args: >
            -Dsonar.plsql.jdbc.url=${{ env.SONAR_JDBC_URL }}
            -Dsonar.plsql.jdbc.user=${{ env.SONAR_JDBC_USER }}
            -Dsonar.plsql.jdbc.password=${{ env.SONAR_JDBC_PASSWORD }}
